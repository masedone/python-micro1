apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: python-reference-micro
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: python-reference-micro.tudominio.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: python-reference-micro
            port:
              number: 80 